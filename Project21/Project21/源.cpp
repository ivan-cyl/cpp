#include <stdio.h>
#include <stdlib.h>
int main()
{
	//int a, b, c;
	int x = 3, y = -4, z = 5;
	printf((x&&y) == (x || z));
	!(x > y) + (y != z) || (x = y) && (y - z);
	x++ - y + (++z);

	system("pause");
	return 0;
}